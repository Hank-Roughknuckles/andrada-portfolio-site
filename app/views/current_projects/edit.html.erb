<% provide( :title, "Edit Project" ) %>

<div class="envelope">
  <%= render "shared/default_grid", :active_record_object => CurrentProject %>
</div>

<%= render "current_projects_form", :content => @content %>
