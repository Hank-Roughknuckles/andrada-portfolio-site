<h3>Edit Work</h3>

<div class="content_preview">
  <%= render "shared/content_lightbox", :content => @content %>
</div>

<div class="envelope">
  <%= render "shared/default_grid", :active_record_object => Work %>
</div>

<%= render "works_form" %>
