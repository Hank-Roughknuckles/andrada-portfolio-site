TO-DO
=====

Make the nav-bar auto adjust spacing based on size of elements so that the navbar always ONLY takes up one line

center navbar within the home screen

Re-enable turbolinks after finishing site and get it to work with everything
  Preview slide isn't showing anything when edit page first loads

install ack and ack-vim

Make slide preview in edit form scaled down so everything fits in one page.  In other words, make the preview be a smaller version of the slide

Change color of save button when an element is edited, prompting the user to save


REFACTOR
========

Make methods that show / hide the save reminder in nav bar and about me contents edit DRY
