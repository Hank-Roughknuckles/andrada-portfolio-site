<% provide( :title, "Works" ) %>

<% for content in @contents  %>
  <%= render "work_content", :content => content %>
<% end %>

<% if user_signed_in? %>
  <%= link_to 'Add New Item', new_work_path %>
<%= render "works_edit_table", contents: @contents %>
<% end %>
