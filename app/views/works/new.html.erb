<% provide( :title, "New Work" ) %>

<div class="invisible current_element_id">
  <%= @content.id %>
</div>

<h3>Add New Work</h3>

<div class="envelope">
  <%= render "shared/default_grid", :edit_mode => true, :active_record_object => Work  %>
</div>

<%= render "works_form" %>
