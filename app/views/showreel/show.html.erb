<% provide( :title, "Showreel" ) %>

<%= render "embedded_video", :content => @content %>

<%= @content.header %>
<%= @content.description %>

<% if user_signed_in? %>
  <div class="edit_showreel_content">
    <%= link_to "Edit Content", edit_showreel_path(@content) %>
  </div>
<% end %>
