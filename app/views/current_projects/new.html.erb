<% provide( :title, "Add New Project" ) %>

<h1>New Project</h1>

<%= render "project_content", :project => @project %>


<%= form_for @project do |f| %>
  <%= render "shared/default_record_form", :f => f %>

  <%= f.label :progress, "Progress (%)" %>
  <%= f.number_field :progress, min: 0, max: 100, step: 1 %>

  <%= f.submit "Save" %>
<% end %>
