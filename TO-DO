TO-DO
=====

Change authentication in application_controller to check to see if the user matches what is in the db

when something is changed without saving on the admin page, make a popup appear that says "You have made changes, but have not saved them.  Click the "Save" button at the bottom

Make the background picture loader automatically reload the background image when an image is selected

center navbar within the home screen

make a way for her to change the link names in the navbar
